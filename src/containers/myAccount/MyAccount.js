import React, {useEffect} from 'react';

const MyAccount = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    });
    
    return(

        <h1>My Account</h1>
    )
}

export default MyAccount;