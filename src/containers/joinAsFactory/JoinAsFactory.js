import React, {useEffect} from 'react';

const JoinAsFactory = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    });

    return(

        <h1>Join as Factory</h1>
    )
}

export default JoinAsFactory;