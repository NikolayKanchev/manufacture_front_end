import React, {useEffect} from 'react';

const Projects = () => {

    useEffect(() => {
        window.scrollTo(0, 0);
    });
    
    return(

        <h1>Projects</h1>
    )
}

export default Projects;